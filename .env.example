# ─── QUEUE ────────────────────────────────────────────
# Credentials used by the RabbitMQ container on first boot
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=changeme

# Connection URL used by the orchestrator service
RABBITMQ_URL=amqp://admin:changeme@rabbitmq:5672

# Queue configuration consumed by queueConsumer.service.ts
RABBITMQ_QUEUE=worker-tasks
RABBITMQ_PREFETCH=5

# Dedicated user for external/third-party access (AMQP + management API),
# exposed directly through nginx. Read/write on messages, no topology changes.
RABBITMQ_API_USER=api-operator
RABBITMQ_API_PASS=changeme-too

# Host ports nginx publishes for direct third-party RabbitMQ access
RABBITMQ_AMQP_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# ─── WORKERS ───────────────────────────────────────────
# Path to the workers.yaml registry (absolute path inside the container)
WORKERS_CONFIG_PATH=/app/workers.yaml
